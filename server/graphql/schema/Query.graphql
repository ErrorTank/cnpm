# import Node from "./interface.graphql"

type User implements Node{
    id: ID!
    fullname: String!
    email: String!
    phone: String!
    password: String!
    gender: Boolean!
    dob: String!
    updatedAt: String!
    createdAt: String!
}

type Query {
    getAuthenUser: User
}
